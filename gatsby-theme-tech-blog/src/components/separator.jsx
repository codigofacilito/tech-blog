/** @jsx jsx */
import React from 'react';

import { jsx } from 'theme-ui'

export default (props) => {
  return(
    <div 
      sx={{
        borderColor: 'gray.2',
        borderTopWidth: '2px',
        borderTopStyle: 'solid',
        my: 20
      }}
    ></div>
  )
}